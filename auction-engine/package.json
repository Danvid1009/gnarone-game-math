{
  "name": "auction-engine",
  "version": "1.0.0",
  "description": "Storage-auction ladder: the stepper's fair crash ladder with a one-shot locker reveal on the hammer (exact, strategy-proof RTP)",
  "type": "module",
  "engines": { "node": ">=20" },
  "bin": { "build-auction": "bin/build-auction.js" },
  "scripts": {
    "test": "node --test",
    "build": "node bin/build-auction.js",
    "simulate": "node bin/simulate.js",
    "fixtures": "node scripts/make-fixtures.js",
    "plot": "python3 scripts/plot.py",
    "examples": "sh scripts/make-examples.sh"
  },
  "license": "UNLICENSED",
  "private": true
}
